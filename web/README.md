# web/

Vue 3 + Vite + TypeScript 前端。

```bash
npm install
npm run dev    # :5173 首页 Mock 行情（无需后端）
npm run build  # → dist/
```

当前首页为 **Mock 数据**，含：币价表、宏观卡片、股指条；约 2.5s 随机抖动价格便于看闪烁效果。

契约：`../docs/RFC-002-api-contract.md`
